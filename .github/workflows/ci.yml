name: CI

on: [push, pull_request]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: ragwiki
          POSTGRES_PASSWORD: ragwiki
          POSTGRES_DB: ragwiki_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - run: pip install -e ".[dev]"
      - run: ruff check --output-format=github .
      - run: ruff format --check .
      - run: mypy .
      - run: pytest --cov=ragwiki --cov-report=xml --cov-fail-under=60 --junitxml=results/test-results.xml
        env:
          DATABASE_URL: postgresql+asyncpg://ragwiki:ragwiki@localhost:5432/ragwiki_test

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: results/test-results.xml

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage.xml
